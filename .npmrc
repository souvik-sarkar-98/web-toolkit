# Scoped packages publish to / install from the public npm registry (no token for install).
# Publisher: run `npm login` once (needs access to the @web-toolkit org on npmjs.com).
@web-toolkit:registry=https://registry.npmjs.org/

